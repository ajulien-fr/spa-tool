<form method="post" action="{{ path('action_delete', {'id': action.id}) }}" onsubmit="return confirm('Êtes vous certain de vouloir supprimer cette Action ?');">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ action.id) }}">
	<button class="btn btn-danger btn-block mt-3">Supprimer</button>
</form>
